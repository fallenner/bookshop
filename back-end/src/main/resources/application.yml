server:
  port: 8080
  #ssl:
    #    证书路径
   # key-store: classpath:www.jsdai.club.jks
   # keyStoreType: JKS
   # key-alias: tomcat
    #    配置密码，就是在生成证书的时候输入的密码
  #  key-store-password: jsdai123

spring:
  datasource:
    url: jdbc:mysql://localhost:3306/bookstore?useSSL=false
    username: root
    password: dzq@1234
    driver-class-name: com.mysql.jdbc.Driver
    type: com.zaxxer.hikari.HikariDataSource
    hikari:
      minimum-idle: 5
      maximum-pool-size: 20
      auto-commit: true
      idle-timeout: 30000
      max-lifetime: 1800000
      connection-timeout: 30000

# Logger Config
logging:
  level:
    com.baomidou.mybatisplus.samples.quickstart: debug